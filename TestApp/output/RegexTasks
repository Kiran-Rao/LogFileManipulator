            StringBuilder output = new StringBuilder(input.Length + 1);
            StringBuilder output = new StringBuilder(input.Length + 1);
                    //int i = 0;
                    for (int i = 0; i < m.Groups.Count; i++)
